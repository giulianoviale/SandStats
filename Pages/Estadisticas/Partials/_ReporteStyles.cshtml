@* sin modelo *@
<style>
    /* ===== Semáforo 0–39 rojo | 40–79 amarillo | 80–100 verde ===== */
    .ef-high {
        background: #d1fae5 !important;
        color: #065f46 !important;
        font-weight: 700;
        border-radius: .3rem;
    }

    .ef-mid {
        background: #fef9c3 !important;
        color: #92400e !important;
        font-weight: 700;
        border-radius: .3rem;
    }

    .ef-low {
        background: #fee2e2 !important;
        color: #991b1b !important;
        font-weight: 700;
        border-radius: .3rem;
    }

    td .ef-high, td .ef-mid, td .ef-low {
        display: inline-block;
        padding: .06rem .35rem;
    }

    /* ===== Cards por jugador ===== */
    .player-a {
        background: #f0f9ff;
    }

    .player-b {
        background: #faf5ff;
    }

    .player-card {
        border-left-width: 8px !important;
        border-left-style: solid !important;
    }

    .player-a.player-card {
        border-left-color: #38bdf8;
    }

    .player-b.player-card {
        border-left-color: #a78bfa;
    }

    /* ===== Panelitos ===== */
    .ex-panels .panel {
        border: 1px solid #dbeafe;
        background: #f8fbff;
        border-radius: .65rem;
        padding: .6rem .7rem
    }

    .ex-panels .head {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: .35rem
    }

    .ex-panels .tit {
        font-weight: 700;
        font-size: .95rem
    }

    .ex-panels .totbox {
        text-align: right
    }

    .ex-panels .totlbl {
        font-size: .8rem;
        color: #6b7280;
        display: block;
        line-height: 1
    }

    .ex-panels .totrow {
        display: flex;
        gap: .5rem;
        align-items: baseline;
        justify-content: flex-end
    }

    .ex-panels .pct {
        font-weight: 800;
        color: #475569
    }

    .ex-panels .cnt {
        font-weight: 700
    }

    .ex-panels .bar {
        border-top: 6px solid #38bdf8;
        margin: .4rem 0 .5rem
    }

    .ex-panels .grid {
        display: grid;
        grid-template-columns: repeat(2,1fr);
        gap: .28rem
    }

    .ex-panels .cell {
        border: 1px solid #e5e7eb;
        border-radius: .35rem;
        padding: .28rem .32rem;
        text-align: center
    }

        .ex-panels .cell small {
            display: block;
            color: #6b7280;
            font-size: .72rem;
            line-height: 1
        }

        .ex-panels .cell b {
            display: block;
            font-size: .9rem
        }

    .ex-panels .dp {
        background: #e7f0ff
    }

    .ex-panels .p {
        background: #e8f8ee
    }

    .ex-panels .n {
        background: #fff6d6
    }

    .ex-panels .e {
        background: #ffe7e7
    }

    .ex-panels .blk-head {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin: .45rem 0 .25rem
    }

    .ex-panels .blk-tit {
        font-weight: 700
    }

    .ex-panels .badge-ef {
        font-weight: 800;
        border-radius: .4rem;
        padding: .05rem .4rem;
        background: #f1f5f9;
        border: 1px solid #e2e8f0
    }

    /* Mesas */
    .table thead th {
        white-space: nowrap;
    }

    /* ===== ATAQUE: columnas coloreadas ===== */
    .table-attack thead th.col-hash, .table-attack td.col-hash {
        background: #e0f2fe;
    }

    .table-attack thead th.col-plus, .table-attack td.col-plus {
        background: #e8f8ee;
    }

    .table-attack thead th.col-minus, .table-attack td.col-minus {
        background: #fff6d6;
    }

    .table-attack thead th.col-eq, .table-attack td.col-eq {
        background: #ffe7e7;
    }

    /* Chips compactos */
    .chip {
        display: inline-flex;
        align-items: center;
        gap: .35rem;
        border: 1px solid #e5e7eb;
        background: #fff;
        border-radius: .5rem;
        padding: .18rem .5rem;
        font-size: .9rem;
    }

        .chip .cnt {
            font-weight: 700;
            color: #334155
        }

        .chip .ef {
            font-weight: 800;
        }
</style>
